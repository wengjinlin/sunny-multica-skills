{{PROJECT_NAME}}

> 本描述只做指针，不维护项目细节——一切以仓库文档为唯一权威源。仓库结构变更后此处无需修改。

- 仓库：{{REPO_URL}}
- 仓库根 `CLAUDE.md`：统一宪法，agent 开工必读（技术栈 / 分层 / 命名 / 保护目录）
- `AGENTS.md`：协作总纲（角色职责 / stage 编号字典 / Git 策略 / 变更联动）
- `docs/`（导航入口 `docs/index.md`）：架构总览与模块定位字典（`architecture/index.md`）、隐性约定（`architecture/implicit-contracts.md`）、产品规则（`product/index.md`）、规范详单（`standards/api|database|testing.md`）、数据库表清单（`database/`）、平台能力帮助（`help/`）、经验教训（`lessons/` 目录，按角色分文件）

单个 change 的实际流程以其 issue 描述为唯一执行权威。
