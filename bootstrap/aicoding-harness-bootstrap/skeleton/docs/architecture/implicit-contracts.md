# 隐性约定（Implicit Contracts）

> 本文档记录项目中**代码不会强制、但约定俗成**的规则。新增任务前必读；探查发现新约定必须补充。与 `CLAUDE.md` 互为补充：CLAUDE.md 是强制约束，本文档是隐性约定。

---

## 1. 拼音/缩写业务对照表（持续维护）

> 若业务路径大量使用拼音缩写，新增任务前**必须**查本表，禁止幻觉式重命名；发现新缩写**必须**补到本表。

| 缩写 | 中文业务 | 出现位置 | 状态 |
|---|---|---|---|
| {{ABBREV_ROW}} | {{...}} | {{...}} | {{...}} |

<!-- 分析指引：从业务包名扫描归纳（每条附真实路径）；业务路径无拼音缩写现象的项目，在本节注明「不适用」并保留维护规则以备后用 -->

### 维护规则

- 发现新缩写：在表格新增一行，**必须**填「出现位置」列
- 发现缩写含义变化：更新「中文业务」列，并在文末追加变更日志
- 禁止删除已确认条目，只能标注「已废弃」

---

## 2. {{OTHER_IMPLICIT_CONTRACTS}}

<!-- 分析指引：其余隐性约定按类追加小节（状态字段取值约定 / 枚举含义 / 特殊路由规则 / 前后端字段映射等），每类一个小节；每条约定须附代码证据（抽样 ≥3 例） -->
