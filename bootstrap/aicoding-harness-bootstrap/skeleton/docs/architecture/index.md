# 架构总览

> 模块定位字典 + 架构事实。**需求落业务域、代码落模块、表结构定位都从本文件进**。
> 宪法级红线见仓库根 CLAUDE.md；隐性约定（命名/状态语义/历史包袱）见 implicit-contracts.md。

## 1. 业务模块对应表（模块定位字典）

| 业务域 | 模块（代码目录） | 数据库主表前缀 | 说明 |
|---|---|---|---|
| {{DOMAIN_MODULE_ROW}} | {{...}} | {{...}} | {{...}} |

<!-- 分析指引：模块定位的权威字典——业务域清单与 product/index.md 一致，模块列从代码目录（如 modules/<name>）扫描，表前缀从表名前缀归纳，三方对齐生成；无法归属业务域的模块归「通用能力」域；无数据库的项目删「表前缀」列 -->

**用法**：拿到需求 → 在 `../product/index.md` 确定业务域 → 查本表定位模块目录与表前缀 → 再进代码 / `../database/tables/<域>.md` 表详情。

## 2. 模块内部约定

{{MODULE_INTERNAL_LAYOUT}}

<!-- 分析指引：模块内固定子结构（如 modules/<module>/{controller,dto,dao,entity,service}）与强制规则（新模块必须照搬的结构、禁止的变体）；从 ≥3 个现有模块抽样归纳；无统一结构则写「（无统一约定，按 CLAUDE.md §8 分层规则）」 -->

## 3. 分层依赖方向

{{LAYER_DEPENDENCY}}

<!-- 分析指引：各层/各模块允许的依赖方向（箭头图或文字链路）；与 CLAUDE.md §8 分层规则同源，此处画全模块级方向图 -->

## 4. 高风险区域

| 区域 | 风险 | 动工前置条件 |
|---|---|---|
| {{HIGH_RISK_ROW}} | {{...}} | {{...}} |

<!-- 分析指引：改错影响面大的位置——配置文件、公共工具/父包、网关路由、核心调度、大数据量表、鉴权链路、全量调用方引用的接口等；「前置条件」写必须的评审（如 design.md 显式声明 + 人审 + DBA 评审）；本表与 CLAUDE.md §10 保护目录互补（彼表是写入拦截，本表是逻辑风险） -->

## 5. 架构决策记录（ADR）

{{ADR_ENTRIES}}

<!-- 分析指引：重大技术决策沉淀，格式：ADR-编号 / 日期 / 决策 / 原因 / 影响；初始为空写「（暂无）」，后续重大决策（选型变更/规范例外）随时追加，只增不改 -->

## 6. 已知技术债

- [ ] {{TECH_DEBT_ITEM}}

<!-- 分析指引：已知未修的问题清单（如深度分页未实现、无测试目录、无链路追踪），每项注明影响面；修复后在对应 change 中勾掉并注明 change-id -->

## 7. 部署架构

{{DEPLOY_ARCHITECTURE}}

<!-- 分析指引：环境/节点/调用拓扑（ASCII 图或文字，含 context-path / 网关 / 注册中心等对外可见路径要素）；简单单体项目可删本节 -->
