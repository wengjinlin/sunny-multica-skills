# 平台能力使用指南

> 本目录收录本项目使用的平台能力（自研/二方平台包 + 通用中间件）的使用指南。任何涉及平台集成的需求，需先查阅对应章节。

## 能力清单

| 能力 | 包 / 组件 | 简介 | 文档 |
|------|----------|------|------|
| {{CAPABILITY_ROW}} | {{...}} | {{...}} | {{...}} |

<!-- 分析指引：从二方包依赖清单（如 com.sunny:* / @org-base-web/*）提取能力→包映射，每个能力一个文档链接；无平台包的项目本表注明「（暂无）」，后续随集成补充 -->

## 共享底座

{{SHARED_BASE_PACKAGES}}

<!-- 分析指引：所有能力共同依赖的基础包/组件（父包、鉴权、i18n、日志、缓存增强等），逐条列出 -->

## 通用约定

适用于**所有**平台能力调用：

1. **封装位置**：平台调用的封装**必须**放在专属封装目录；新模块中调用**必须**通过封装层，**禁止**直接 new 客户端
2. **异常处理**：平台调用的异常**必须**在 Service 层捕获并转换为业务异常，**禁止**向上抛裸异常
3. **调用位置**：平台调用**禁止**写在 Controller / 展示层
4. **超时 / 重试 / 降级**：策略**必须**在 OpenSpec `design.md` 中显式说明（读超时 / 连接超时 / 重试次数 / 降级返回）
5. **配置**：连接信息走配置中心/配置文件，**禁止**硬编码；敏感配置走加密项
6. **日志**：调用入口 / 出口 / 失败**必须**有日志，含操作者 + method + 耗时
7. **幂等**：写操作的平台调用**必须**有业务幂等键，**禁止**靠重试兜底

## 选型决策记录

{{SELECTION_ROWS}}

<!-- 分析指引：自研包 vs 社区组件的对照与选择原因（有才写，无则删本节）。含义：新需求优先复用现有平台包，禁止引入功能重叠的新依赖（除非 design.md 显式评估） -->

## 受保护 / 慎用清单

| 区域 | 风险 | 注意 |
|------|------|------|
| {{RISK_ROW}} | {{...}} | {{...}} |

<!-- 分析指引：按实际中间件填（升级影响 / 凭据泄露 / 超时耗尽 / 死信重复消费 / 锁 TTL 等）；本表与「通用约定」视同宪法红线，违反会被 Reviewer 打回 -->
